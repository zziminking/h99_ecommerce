spring:
  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트마다 스키마 재생성
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

  # Testcontainers가 자동으로 datasource 설정
