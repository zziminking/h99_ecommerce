spring:
  profiles:
    active: test

  datasource:
    url: jdbc:mysql://localhost:3306/h99_ecommerce?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: qlalf1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트마다 스키마 재생성
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    h99.ecommerce.aspect.DistributedLockAspect: DEBUG  # 분산 락 로그 활성화
